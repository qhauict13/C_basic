#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#define MAX 100
typedef struct {
	char model[20];
	char memory[10];
	char screen[10];
	char price[15];
}
void import(FILE *fin, FILE *fout){
	char buff[MAX];
	while()
}